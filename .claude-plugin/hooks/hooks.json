{
  "SessionStart": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "vega doctor --json --quick || true",
          "description": "Run vega doctor at session start to surface bundle missing / stale before the agent first invokes vega tf. Failure is non-fatal; the SKILL.md instructs the agent to fall back to telling the user to run `vega install`."
        }
      ]
    }
  ]
}
