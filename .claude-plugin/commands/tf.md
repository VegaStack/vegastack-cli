---
name: tf
description: Search Terraform docs (vega tf <query>) — local, deterministic, no network.
allowed-tools: Bash(vega:*), Bash(jq:*), Read, Grep
---

Run `vega tf "$ARGUMENTS"` and read the returned JSON envelope. Cite `citations[]` in your reply. See `skills/terraform-docs/SKILL.md` for the full mental model and the four-channel envelope.
