---
name: tf
description: Search Terraform docs (vegastack tf <query>) — local, deterministic, no network.
allowed-tools: Bash(vegastack:*), Bash(jq:*), Read, Grep
---

Run `vegastack tf "$ARGUMENTS"` and read the returned JSON envelope. Cite `citations[]` in your reply. See `skills/terraform-docs/SKILL.md` for the full mental model and the four-channel envelope.
