{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "vegastack doctor --json --quick || true",
            "description": "Run vegastack doctor at session start to surface bundle missing / stale before the agent first invokes vegastack tf. Failure is non-fatal; the SKILL.md instructs the agent to fall back to telling the user to run `vegastack install`."
          }
        ]
      }
    ]
  }
}
