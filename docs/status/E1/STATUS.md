# E1 — Manifest fixer — STATUS

**Status:** done.
**Tests:** 12/12 passing (`python3 -m pytest tests/manifest_builder/`).
**Validation:** 31/31 per-provider MANIFEST.json files validate against `schema/manifest.schema.json`.
**Time:** ~70 minutes.

## What changed (file list with LOC delta)

| Path | Action | LOC delta |
|---|---|---|
| `terraform-providers/scripts/manifest_builder.py` | Major rewrite (extract_schema state machine, F1/F2/F15/F17/F21/F23 fixes, schema v1) | +400 / -180 |
| `terraform-providers/scripts/build_companions.py` | NEW (closes F10) | +145 |
| `terraform-providers/scripts/build_aliases.py` | NEW (closes F4) | +130 |
| `terraform-providers/scripts/validate_manifest.py` | NEW (Draft 2020-12 validator) | +95 |
| `terraform-providers/schema/manifest.schema.json` | Copied verbatim from /tmp/synthesis/contracts/ + 1 char patch (provider regex; see contract-issue.md) | +204 |
| `terraform-providers/aws/companions.yaml` | Seed (10 entries) | +50 |
| `terraform-providers/cloudflare/companions.yaml` | Seed (5 entries) | +25 |
| `terraform-providers/kubernetes/companions.yaml` | Seed (4 entries) | +20 |
| `terraform-providers/vault/companions.yaml` | Seed (2 entries) | +15 |
| `terraform-providers/cloudflare/aliases.yaml` | Seed (4 entries) | +25 |
| `terraform-providers/tests/manifest_builder/test_extract_schema.py` | NEW (12 tests) | +220 |
| `terraform-providers/tests/manifest_builder/fixtures/sdkv2_with_nested_block.md` | Test fixture | +60 |
| `terraform-providers/tests/manifest_builder/fixtures/plugin_framework_with_nested_schema.md` | Test fixture | +65 |
| `terraform-providers/tests/manifest_builder/fixtures/sdkv2_with_deprecated.md` | Test fixture | +20 |
| `terraform-providers/{aws,cloudflare,kubernetes,vault}/aliases.json` (cloudflare only — generated) | Generated by build_aliases.py | +1 (compact) |
| All 31 `terraform-providers/<provider>/MANIFEST.json` | Rebuilt with schema v1; clean required_args | regenerated |

Net: ~+1,400 lines authored (excludes regenerated MANIFEST.json blobs).

## Web-searched packages (and pinned versions)

| Package | Searched | Pinned |
|---|---|---|
| `jsonschema` (Python) | 4.26.0 latest stable; we installed `>=4.18,<5` (system pip's index capped at 4.25.1; both support Draft 2020-12 natively via `Draft202012Validator`) | `>=4.18,<5` |
| `PyYAML` | 6.0.3 latest (Sep 2025); already installed at 6.0.3 system-wide | `>=6.0` |
| `pytest` | 9.0.3 latest (Apr 2026) but dropped Py 3.9; we pin 8.x for Py 3.9 compat | `>=8.3,<9` |
| Python target | Confirmed 3.9.6 system; spec says "bump to 3.9+" — done | `>=3.9` |

## F-finding closure status

- **F1** (sdkv2 nested-block leak into top-level required): FIXED — `aws_db_instance.required_args` now `[allocated_storage, engine, instance_class, username]` (was 8). `aws_lb_listener.required_args` now `[default_action, load_balancer_arn]` (was 26). `aws_eks_cluster.required_args` now `[name, role_arn, vpc_config]` (was 12). `aws_security_group.required_args` now `[]` (was 6).
- **F2** (Plugin Framework `### Nested Schema for X` bug + schema_origin detection): FIXED — `cloudflare_zero_trust_access_application` top-level `optional_args` now 39 clean entries; nested `cors_headers`, `destinations`, `policies` etc. captured under `blocks: {}`. Every resource carries `schema_origin: "sdkv2" | "plugin_framework" | "mixed"`.
- **F10** (`recommended_companions` field empty): FIXED — `build_companions.py` reads `<provider>/companions.yaml`, validates, merges. Seeded 4 example providers.
- **F15** (enum hint regex spans hundreds of lines): FIXED — `ENUM_LINE_RE` is per-line with 200-char arg-backtick window.
- **F17** (single-process build): FIXED — `--all <root>` uses `multiprocessing.Pool(processes=os.cpu_count())`. 31 providers built in ~1s.
- **F21** (no description-token index): FIXED — `description_token_index` emitted (~1,200 tokens for AWS).
- **F23** (`GENERIC_ARGS` drop set): FIXED — replaced with `generic_args_downweight: {name: 0.25, id: 0.10, ...}` map; emitted in MANIFEST.

## What's BLOCKED on another team

Nothing blocked. Downstream:
- **E2 (CLI harness):** consumes `recommended_companions`, `description_token_index`, `generic_args_downweight`, `service_aliases`, `blocks`, `schema_origin`. All present in the manifests.
- **E3 (content authoring):** will replace my 4 seed `companions.yaml` files with the full ~30-entry set per `/tmp/synthesis/v1-plan.md` §10.4.
- **E6 (eval harness):** can now write `manifest_check` expectations against clean `required_args`.

## Contract issue raised

See `/tmp/exec-status/E1/contract-issue.md`. Short version: the `provider` regex `^[a-z][a-z0-9-]*$` rejects `1password` (the canonical name of one of our 31 providers). Patched the bundle's local copy to `^[a-z0-9][a-z0-9-]*$`. Source of truth at `/tmp/synthesis/contracts/manifest.schema.json` should be updated by orchestrator so E2's TS loader stays in sync.

## What's left for the audit team to verify

1. Re-run all builders end-to-end: `python3 scripts/manifest_builder.py --all . --bundle-version 2026.04.28 && python3 scripts/build_companions.py . && python3 scripts/build_aliases.py . && python3 scripts/validate_manifest.py --all .`
2. Confirm the four headline `required_args` numbers via the `jq` queries listed in the brief.
3. Confirm `schema_origin` appears on every resource: `jq -r '.resources | to_entries[] | "\(.key) \(.value.schema_origin)"' aws/MANIFEST.json | head`
4. Confirm `recommended_companions` non-empty for the 21 seeded entries.
5. Cross-check that E2's TS loader doesn't break on the new `blocks: {...}` field (additive, but make sure the type hasn't drifted).

## Build / typecheck / lint

- `python3 -m pytest tests/manifest_builder/` → exit 0, 12 passed.
- `python3 scripts/manifest_builder.py --all .` → exit 0, 31/31.
- `python3 scripts/validate_manifest.py --all .` → exit 0, 31/31 PASS.
- `python3 scripts/build_companions.py .` → exit 0, 21 entries merged (1 missing-ref warning, expected — `cloudflare_zone_settings_override` was renamed upstream).
- `python3 scripts/build_aliases.py .` → exit 0, 4 alias entries written.

## Headline numbers (from brief deliverable)

```
$ jq '.resources.aws_db_instance.required_args | map(.name)' aws/MANIFEST.json
[ "allocated_storage", "engine", "instance_class", "username" ]   # was 8

$ jq '.resources.aws_lb_listener.required_args | map(.name)' aws/MANIFEST.json
[ "default_action", "load_balancer_arn" ]                          # was 26

$ jq '.resources.aws_eks_cluster.required_args | map(.name)' aws/MANIFEST.json
[ "name", "role_arn", "vpc_config" ]                               # was 12

$ jq '.resources.cloudflare_zero_trust_access_application.required_args | map(.name)' cloudflare/MANIFEST.json
[ ]   # the upstream PF doc has zero ### Required entries; account_id is under ### Optional
       # (mutually exclusive with zone_id). Crucially, optional_args dropped from polluted
       # 39+nested-leakage to clean 39 + 9 properly-scoped blocks.
```
