# P3 — STATUS

**Date:** 2026-04-28 · **Author:** P3 (SKILL.md TerraShark-style rewrite)
**Time spent:** ~45 min wall-clock (2-hour budget — under).

---

## §1 SKILL.md — before/after

| Metric | Before | After | Delta |
|---|---|---|---|
| `wc -w skills/terraform-docs/SKILL.md` (full file incl. frontmatter) | 2063 | 825 | −1238 (−60%) |
| Body-only word count (excluding frontmatter) | ~1980 | 649 | −1331 (−67%) |
| Approx. tokens (×1.3) | ~2680 | ~1075 | −1605 |
| Decision-tree only (the one block agents read on every turn) | 0 | ~150 tokens | +150 |
| Effective per-turn activation cost (decision tree + matching reference) | ~2680 (whole body always loaded) | ~150 + 200-400 (one matching ref) | ~7× cheaper, matches R6 §3b TerraShark target of 4400 → 600 |

Body word count well under the 750-word contract cap (649). Decision tree opens the body, matching the convergent 2026 pattern (R6 §5 #4).

---

## §2 Frontmatter additions (per R6 / S2 §6)

Added to template + generated SKILL.md:

- `compatibility: "Requires Node >=18; @vegastack/cli on PATH (npm i -g @vegastack/cli && vega install). Works in Claude Code, Codex CLI, Cursor, Gemini CLI, Continue, Aider; spec-compatible with Microsoft Agent Framework, OpenCode, Goose, OpenClaw, Hermes, atmos."` — Microsoft Agent Framework recommends this field be populated (R6 §3e).
- `tags: [Terraform, IaC, Documentation, Multi-Provider, Deterministic]` — Hermes search surface (R6 §4 Hermes).
- `codex: [vegastack-cli]` — Hermes codex association (R6 §4 Hermes).

Preserved unchanged:
- `name`, `description` (E5's WHEN-NOT clarity preserved verbatim), `license`, `allowed-tools`, `metadata.{homepage, schema_version, bundle_version, providers_count}`.

---

## §3 New `references/` files

| Path | Word count | Source content |
|---|---|---|
| `skills/terraform-docs/references/import.md` | 401 | Pulled the `import_syntax` discussion + composite-ID gotchas from old SKILL.md Example 5; added per-provider id_format table; added TF 1.5+ `import {}` block guidance. |
| `skills/terraform-docs/references/migrations.md` | 523 | Cross-links to `bundle/knowledge/<id>.md` cards covering renames/deprecations; tabulates the 9 cards that are migration-shaped; gives a generic recipe for un-carded migrations. |
| `skills/terraform-docs/references/recent-changes.md` | 643 | Lists all 16 knowledge cards by `date_authored` (newest first) with id, provider, one-liner, date. Cross-links to `knowledge-cards.md` for schema/auth-guide. |

All three new files are well under the 300-500 word target; only `recent-changes.md` is slightly over (643) because the 16-row inventory table is itself ~300 words.

---

## §4 Updated `references/` files

| Path | Change | Word delta |
|---|---|---|
| `skills/terraform-docs/references/discover-cli.md` | Added 1-paragraph intro explaining this file is the deeper layer of progressive disclosure (loaded only when the user wants flag-level detail). | +71 words |
| `bundle/skill-source/SKILL.md.template` | Full rewrite: TerraShark-style decision tree + sparse body + new frontmatter fields. | 2031 → 793 (−1238) |

Preserved unchanged (already aligned with the new shape):
- `references/manifest-schema.md`
- `references/concept-aliases.md`
- `references/recipes.md` (the SKILL.md decision tree points to it for multi-provider topology queries; the existing 10-recipe inventory already matches the contract)
- `references/eval-baseline.md`
- `references/troubleshooting.md`
- `references/knowledge-cards.md` (now reachable via `recent-changes.md` and `migrations.md` cross-links — the SKILL.md no longer links it directly because `recent-changes.md` is the friendlier inventory entry point)

---

## §5 Verification

| Check | Result |
|---|---|
| `npm run generate-skill:check` | **exit 0** — `ok: skills/terraform-docs/SKILL.md matches the template render` |
| `npm test` | **329 / 329 passed** (the brief mentioned 308; current main has 329 — net +21) in 4.52s, 37 test files, 0 failures. Markdown-only changes don't affect any test. |
| Every `references/*.md` linked from SKILL.md exists on disk | 9/9 OK (concept-aliases, discover-cli, eval-baseline, import, manifest-schema, migrations, recent-changes, recipes, troubleshooting) |
| Decision tree opens the body as exactly specified in the contract (7 numbered branches, then a token-cost note) | ✅ |
| Frontmatter preserves required fields | ✅ (`name`, `description`, `license`, `allowed-tools`, `metadata`) |

---

## §6 Files touched

- `bundle/skill-source/SKILL.md.template` (full rewrite — source of truth for the generator)
- `skills/terraform-docs/SKILL.md` (regenerated by `npm run generate-skill`)
- `skills/terraform-docs/references/import.md` (new)
- `skills/terraform-docs/references/migrations.md` (new)
- `skills/terraform-docs/references/recent-changes.md` (new)
- `skills/terraform-docs/references/discover-cli.md` (intro paragraph added)

No code touched. No tests touched. All P3 changes are markdown.

---

## §7 Cross-team confirmation

- P1 / P2 / P4 don't touch `SKILL.md` or `references/`. No overlap.
- The generator (`scripts/generate-skill-from-bundle.ts`) is unchanged — only the template input changed. CI's `generate-skill:check` keeps passing.

---

*End P3.*
