# 2026-05-08 — hygiene + stale-comment nits

Eight low-impact findings. Worth addressing in a single hygiene commit.

## F9 — Stale audit reference in `src/cli.ts:16`

- **Observed:** comment references "Audit performance/F-002 in audit-1778150875" with no path or link.
- **Fix:** replace with a relative path (`audits/audit-1778150875-2026-05-07T10-47-55Z-full.md`) or drop the marker — the fix has already shipped per `0.1.13-next.1` CHANGELOG.

## F25 — Consumed changeset still present

- **Observed:** `.changeset/audit-drainage-2026-05-07.md` exists and matches the bullet content already shipped in `CHANGELOG.md` under `0.1.13-next.1`. `.changeset/pre.json:changesets` still lists `"audit-drainage-2026-05-07"`.
- **Risk:** next `changeset version` could double-publish the bullet under the next prerelease.
- **Fix:** verify the changeset has been consumed; if it has, remove it from `pre.json:changesets` and delete the `.md` file. If `changeset publish` was run already, this is a leftover that should be pruned.

## F26 — `dist/` committed (cross-listed)

See F26 in `2026-05-08-naming.md`. Either gitignore or document.

## F27 — `.npmignore` redundant (cross-listed)

See F27 in `2026-05-08-naming.md`. Delete in favor of `package.json:files`.

## F28 — `audits/` directory hygiene

- **Observed:** `audits/` is committed; not in `package.json:files` (good — won't ship).
- **Fix:** confirm runtime code paths don't reference `audits/*` (skill, tests). Already verified clean — leave as-is.

## F29 — `apps/`, `evals/`, `exports/` undocumented

- **Where:** `/apps/`, `/evals/`, `/exports/`
- **Observed:** these directories exist at repo root with no mention in README, CONTRIBUTING, or AGENTS.
- **Fix:** one-paragraph "Repo layout" note in `CONTRIBUTING.md` (folds with F22). If `exports/` is generated, add to `.gitignore`.

## F30 — `src/agents/types.ts:42-45` comment out of sync

- **Observed:** comment claims a Gemini install layout that doesn't fully match the modern renderer (see F10).
- **Fix:** reconcile after F10's canonical-layout decision lands.

## F31 — `src/commands/skills.ts:1-3` migration commentary

- **Observed:** comment narrates the legacy → renderer migration (4 → 6 agents). This belongs in a CHANGELOG entry, not in the source file.
- **Fix:** drop the comment; add an Unreleased CHANGELOG bullet referencing the migration (folds with F4).
