{
  "name": "@vegastack/mcp-server",
  "version": "0.1.8",
  "private": true,
  "description": "vegastack-cli Remote MCP server — Cloudflare Workers + Agents SDK. Exposes `vegastack tf` discovery (and friends) to any MCP-aware coding agent over StreamableHTTP/SSE.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "agents": "0.11.6",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.15.0",
    "@cloudflare/workers-types": "4.20260426.1",
    "typescript": "6.0.3",
    "vitest": "4.1.5",
    "wrangler": "4.85.0"
  }
}
